Flask>=3.0,<4.0

# Milestone 2 — Gmail + Calendar connectors (OAuth desktop flow):
google-api-python-client>=2.0
google-auth-oauthlib>=1.0

# Milestone 3 — News RSS + scheduled LLM headline distillation:
feedparser>=6.0
anthropic>=0.40          # Claude Haiku 4.5 headline distillation
python-dotenv>=1.0       # load ANTHROPIC_API_KEY from .env

# Deployment — production WSGI server (Railway runs `gunicorn app:app`):
gunicorn>=21.0

# IANA timezone database for zoneinfo. The calendar pins events to America/New_York
# (connectors/gcal.py); without the system tz data a minimal Linux container (Railway)
# would raise ZoneInfoNotFoundError. Declaring it guarantees the lookup on any host.
tzdata>=2024.1
